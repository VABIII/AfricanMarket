import React from 'react';

const About = (props) => {
    const {test} = props;
    console.log(test);

    return (
        <div>
            <h1>About</h1>
        </div>
    );
};

export default About;

















































